import { Navbar, Container, NavDropdown, Nav, Form, FormControl, Button } from "react-bootstrap";
const PageNavbar = () => {

  return (<Navbar bg="light" expand="lg">
    <Container fluid>
      <Navbar.Brand href="/">Kryptokollen</Navbar.Brand>
    </Container>
  </Navbar>)
};

export default PageNavbar;